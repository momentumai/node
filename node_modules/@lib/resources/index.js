var Promise = require('bluebird'),
    db = require('@lib/resources/db');

module.exports.get = function () {
    return Promise.props({
        'pool': db.createPool()
    });
};

module.exports.dispose = function (r) {
    return Promise.all([db.destroyPool(r.pool)]);
};
