SELECT
 team.id,
 name,
 IF(session.id IS NULL,
   0,
   1
 ) AS active
FROM team
{{#unless is_super}}
    INNER JOIN team_user
    ON team.id = team_user.team_id AND team_user.user_id = {{user_id}}
{{/unless}}
LEFT JOIN session
ON team.id = session.team_id AND session.id = {{session_id}};