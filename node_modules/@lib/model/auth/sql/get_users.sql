SELECT
    user.id,
    email,
    is_admin,
    CASE WHEN user.id = {{user_id}} THEN 1 ELSE 0 END AS me
FROM user
INNER JOIN team_user
ON user.id = team_user.user_id
WHERE team_id = {{team_id}}
ORDER BY me DESC, email ASC;
