SELECT
    1 as active
FROM
    alert
WHERE
    team_id = {{team_id}} AND
    content_id = '{{content_id}}' AND
    until = 0 AND
{{#each cats}}
    cat{{inc @index}} = '{{.}}' AND
{{/each}}
    status IS NULL;
