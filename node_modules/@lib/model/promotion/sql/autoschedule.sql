SELECT
    1 as active
FROM
    rule_action
WHERE
    team_id = {{team_id}} AND
    action_type = 'alert' AND
    content_id = '{{content_id}}' AND
    until = 0 AND
{{#each cats}}
    cat{{inc @index}} = '{{.}}' AND
{{/each}}
    status IS NULL;
