SELECT
    id
FROM
    alert
WHERE
    team_id = {{team_id}} AND
    content_id = '{{content_id}}' AND
    UNTIL = 0 AND
{{#each cats}}
    cat{{inc @index}} = {{.}} AND
{{/each}}
    status IS NULL;
