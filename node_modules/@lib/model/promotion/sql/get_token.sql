SELECT
    campaign.user_id,
    value AS access_token
FROM campaign
INNER JOIN user_data ON campaign.user_id = user_data.user_id
WHERE campaign.team_id = {{team_id}} AND
    campaign.id = {{campaign_id}} AND
    `key` = 'facebook_access_token';
