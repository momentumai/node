INSERT INTO rule (
    team_id,
    cat1,
    cat2,
    cat3,
    definition,
    module,
    name
) VALUES (
    {{team_id}},
    {{cat1}},
    {{cat2}},
    {{cat3}},
    '{{definition}}',
    '{{module}}',
    '{{name}}'
) ON DUPLICATE KEY UPDATE
    id = LAST_INSERT_ID(id);
