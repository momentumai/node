INSERT INTO team_property (
    name,
    team_id,
    parent_id,
    value,
    display_value
)
VALUES (
    '{{name}}',
    {{team_id}},
    {{parent}},
    '{{value}}',
    '{{display}}'
)
ON DUPLICATE KEY
UPDATE value = value;
