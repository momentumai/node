SELECT
    content_id,
    timestamp
FROM
    rule_action
WHERE
    team_id = {{team_id}} AND
    action_type = 'alert' AND
    {{#each cats}}
        cat{{inc @index}} = '{{.}}' AND
    {{/each}}
    timestamp > {{last_start}}
ORDER BY timestamp DESC;