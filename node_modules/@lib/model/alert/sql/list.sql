SELECT
    alert.content_id,
    timestamp
FROM
    alert
WHERE
    alert.team_id = {{team_id}} AND
    {{#each cats}}
        cat{{inc @index}} = '{{.}}' AND
    {{/each}}
    timestamp > {{last_start}}
ORDER BY timestamp DESC;