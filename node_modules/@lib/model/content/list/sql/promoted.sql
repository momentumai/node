SELECT  
    content_id
FROM promotion
WHERE until = 0 AND
    team_id = {{team_id}} AND
    content_id = '{{content_id}}' AND
{{#each cats}}
    cat{{inc @index}} = '{{.}}' AND
{{/each}}
    {{to}} < expire;
