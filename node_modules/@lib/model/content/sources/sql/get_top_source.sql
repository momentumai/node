SELECT
	source,
	SUM(share) as sum
FROM
	source
WHERE
	team_id = {{team_id}} AND
	content_id = {{content_id}} AND
	traffic_type = {{traffic_type}} AND
	timestamp BETWEEN {{from}} AND {{to}}
GROUP BY
	source
ORDER BY sum DESC
LIMIT 1;
