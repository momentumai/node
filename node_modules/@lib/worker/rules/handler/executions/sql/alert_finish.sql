UPDATE rule_action
SET until = {{until}}
WHERE team_id = {{team_id}} AND
    action_type = 'alert' AND
    until = 0 AND
{{#each cats}}
    cat{{inc @index}} = '{{.}}' AND
{{/each}}
    content_id = '{{content_id}}';
