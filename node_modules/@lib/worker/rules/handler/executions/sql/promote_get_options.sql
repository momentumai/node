SELECT
    team_user_data.value AS options,
    user_data.value AS access_token
FROM team_user_data 
INNER JOIN
    user_data
ON team_user_data.user_id = user_data.user_id AND
    user_data.key = 'facebook_access_token'
WHERE team_user_data.key = 'rule_{{rule_id}}' AND
    team_id = {{team_id}};
