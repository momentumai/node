UPDATE rule_action
SET until = {{until}}
WHERE team_id = {{team_id}} AND
    rule_type = 'promotion' AND
    until = 0 AND
{{#each cats}}
    cat{{inc @index}} = '{{.}}' AND
{{/each}}
{{#if rule_id}}
    rule_id = {{rule_id}} AND
{{/if}}
    content_id = '{{content_id}}';
