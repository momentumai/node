SELECT  
    content_id,
    promotion_id,
    campaign_id
FROM promotion
WHERE until = 0 AND
    team_id = {{team_id}} AND
{{#each cats}}
    cat{{inc @index}} = '{{.}}' AND
{{/each}}
    {{to}} < expire;
